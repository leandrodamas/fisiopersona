# AUTOMATICO: npm run db:migrate aplica tudo em sql/migrations.manifest.json (CNS, parceira, curso, RLS, dias/estagiarios text[]).
# Onde copiar: Supabase → Project Settings → Database → Connection string → URI (Postgres + senha).
# Guarde como ficheiro .env nesta pasta. Nunca commite credenciais reais.
DATABASE_URL=postgresql://postgres.[PROJECT]:[PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres

# Deploy Vercel (Settings → Environment Variables): usadas em build para gerar public/config.js
# Podes usar estes nomes ou SUPABASE_URL / SUPABASE_ANON_KEY (iguais aos do painel Supabase).
# FISIO_SUPABASE_URL=https://xxxxx.supabase.co
# FISIO_SUPABASE_ANON_KEY=eyJ...
# FISIO_SENTRY_DSN=
# FISIO_ENVIRONMENT=production
